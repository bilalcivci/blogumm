# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
#
default: &default
  adapter: sqlite3
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  timeout: 5000

development:
  <<: *default
  database: db/development.sqlite3
  # adapter: mysql2
  # database: <%= ENV['TEST_DATABASE'] %> # databaseName
  # username: <%= ENV['TEST_USERNAME'] %>  # userName
  # password: <%= ENV['TEST_PASSWORD'] %> # userPassword;
  # host:     <%= ENV['TEST_HOST'] %>      # awsDbsAddress


# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: mysql2
  database: <%= ENV['TEST_DATABASE'] %> # databaseName
  username: <%= ENV['TEST_USERNAME'] %>  # userName
  password: <%= ENV['TEST_PASSWORD'] %> # userPassword;
  host:     <%= ENV['TEST_HOST'] %>      # awsDbsAddress

production:
  adapter: mysql2
  database: <%= ENV['DATABASE'] %> # databaseName
  username: <%= ENV['USERNAME'] %>  # userName
  password: <%= ENV['PASSWORD'] %> # userPassword;
  host:     <%= ENV['HOST']%>      # awsDbsAddress

#  database: <%= ENV['DATABASE'] %> # databaseName
#  username: <%= ENV['USERNAME'] %>  # userName
#  password: <%= ENV['PASSWORD'] %> # userPassword;
#  host:     <%= ENV['HOST']%>      # awsDbsAddress

